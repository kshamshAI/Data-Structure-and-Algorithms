# Write a program to return the length of longest consecutive sequence out of a list?

def long_seq(arr):
    seq = []
    for i in range(0,len(arr)):
        sequence = arr[i]
        if arr[i+1] == sequence + 1

